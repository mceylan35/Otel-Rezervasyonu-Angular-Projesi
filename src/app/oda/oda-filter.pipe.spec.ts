import { OdaFilterPipe } from './oda-filter.pipe';

describe('OdaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OdaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
